pipeline {
    agent any
    stages {
        stage('Clone repository') {
            steps {
                checkout scm
            }
        }

        stage('start docker-compose'){
            steps {
                sh 'docker-compose -d docker-compose.yml up'
            }
        }
    }
}